config MTK_BOOT
	bool "Enable MTK Boot information Driver"
	default y
	help
	  Boot information driver includes specific information about booting process, such as boot mode.

config LCT_BOOT_REASON
	bool "enable lct boot reason"
	default n
