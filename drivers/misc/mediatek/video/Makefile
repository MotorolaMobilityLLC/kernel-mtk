################################################################################
# FrameBuffer Driver for Display
#
#
################################################################################

######################################
obj-$(CONFIG_MTK_FB) += common/

obj-$(CONFIG_MTK_FB) += $(subst ",,$(CONFIG_MTK_PLATFORM))/
