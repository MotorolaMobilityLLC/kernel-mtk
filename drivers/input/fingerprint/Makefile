#
# Makefile for all kinds of Fingerprint
#

obj-$(CONFIG_FINGERPRINT_GOODIX_GF_TEE) += goodix/
obj-$(FPC_FINGERPRINT) += FPC/

#ifeq ($(CONFIG_FINGERPRINT_GOODIX_GF_TEE),y)
#obj-y	+=  goodix/
#endif

#ifeq ($(FPC_FINGERPRINT),y)
#obj-y	+=  FPC/
#endif
